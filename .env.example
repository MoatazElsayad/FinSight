# Copy this file to `.env` in the project root (or set the same keys in your environment).
# Used by `finsight_gui` via EnvLoader — see `src/gui/main_gui.cpp`.

OPENROUTER_API_KEY=
OPENROUTER_API_URL=https://openrouter.ai/api/v1/chat/completions

EMAIL_ENABLED=false
EMAIL_API_URL=https://api.resend.com/emails
EMAIL_API_KEY=
EMAIL_FROM_EMAIL=
EMAIL_FROM_NAME=FinSight
